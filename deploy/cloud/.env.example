# 药知 MVP · 云服务器部署环境变量示例
# 复制为 .env 并按需修改；docker-compose 会自动读取同目录 .env

# 服务器域名或公网 IP（仅作提示用，nginx 用 server_name _ 通配，无需改）
DOMAIN=your-domain-or-ip

# 后端启动时是否自动建库 + 导入种子（true=每次容器启动重建演示数据）
SEED_ON_STARTUP=true
